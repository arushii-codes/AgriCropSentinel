<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP/HTTPS traffic to your ngrok domain -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">https://fd0d5fedcfba.ngrok-free.app</domain>
    </domain-config>

    <!-- Optional: Allow local testing on localhost -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
